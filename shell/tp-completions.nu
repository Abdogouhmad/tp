# tp complesion

export extern "tp" [
    --help(-h)           # Print help (see a summary with '-h')
    --generate(-w)       # Generate a simple configuration
    --clean              # clean the back up configuration
    --check(-c): path    # validate the configuration you wrote
]
